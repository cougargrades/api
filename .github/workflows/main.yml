name: "Firebase CI"
on: [push]
jobs:
  ci:
    name: Build, lint, and test on Node ${{ matrix.node }} and ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node: ['14.x']
        os: [ubuntu-latest, windows-latest, macOS-latest]
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - uses: actions/setup-node@v2
        with:
          node-version: '14.x'
      - name: Cache Firebase Emulator JARs
        uses: actions/cache@v2
        with:
          path: |
            ~/.cache/firebase/emulators/
            ~/.npm-packages/lib/node_modules/firebase-tools
          key: ${{ runner.os }}-${{ hashFiles('functions/package-lock.json') }}
      - name: Install deps (with cache)
        uses: bahmutov/npm-install@v1
        with:
          working-directory: functions
      - name: Build
        run: npm run build
        working-directory: functions
      - name: wtf
        run: |
          ls -la
          ls -la lib
        working-directory: functions
      - name: Install http://firebase.tools
        run: npm install -g firebase-tools@9.14.0
      - name: Run tests inside Firebase Emulator
        working-directory: functions
        run: npm test
      # - name: Run tests inside Firebase Emulator
      #   uses: w9jds/firebase-action@v2.0.0
      #   with:
      #     args: firebase emulators:exec 'npm test'
      #     working-directory: functions
